{
  "name": "sourcedcs-web",
  "version": "1.0.0",
  "description": "SOURCE DCS main website — Node.js / Express server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build:frontend": "cd frontend && npm ci && npm run build"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "multer": "^2.1.1"
  }
}
